/**
 * 还原别名
 * @param source 
 * @returns 
 */
export const restoreAlias = (source: string) => source
// .replace(
//   /import(.+)from\s['|"]@vunk\/components\/(.+)['|"]/g, 
//   function(a, p1, p2){
//     return `import${p1}from 'vunk'`
//   }
// )
